#!/usr/bin/env python3
"""Module which has a function to concatenate two strings and returns a single
String value"""


def concat(str1: str, str2: str) -> str:
    """adds two strings together and returns a joint string"""
    return str1 + str2
